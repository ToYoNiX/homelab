services:
  librespeedtest:
    container_name: speedtest
    image: ghcr.io/librespeed/speedtest:latest
    restart: always
    networks:
      - trusted_proxy
    environment:
      MODE: standalone

networks:
  trusted_proxy:
    external: true
